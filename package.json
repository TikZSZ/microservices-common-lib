{
  "name": "@tikzsztickets/common",
  "version": "1.0.4",
  "license": "MIT",
  "main": "./build/index.js",
  "types": "./build/index.d.ts",
  "files": [
    "build/**/*"
  ],
  "scripts": {
    "clean": "del ./build/* ",
    "build": "yarn clean && tsc",
    "pub": "git add . && git commit -m 'Updates' && yarn build && npm version patch && npm publish "
  },
  "devDependencies": {
    "del-cli": "^4.0.1",
    "typescript": "^4.3.5"
  }
}
