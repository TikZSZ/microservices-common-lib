{
  "name": "@tikzsztickets/common",
  "version": "1.0.6",
  "license": "MIT",
  "main": "./build/index.js",
  "types": "./build/index.d.ts",
  "files": [
    "build/**/*"
  ],
  "scripts": {
    "clean": "del ./build/* ",
    "build": "yarn clean && tsc",
    "prepare": "git add . && git commit -m 'Updates' && yarn build && npm version patch && npm publish ",
    "pub": "npm run prepare"
  },
  "devDependencies": {
    "del-cli": "^4.0.1",
    "typescript": "^4.3.5"
  }
}
